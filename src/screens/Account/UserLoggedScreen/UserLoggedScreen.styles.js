import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btnStyles: {
        marginTop: 30,
        paddingVertical: 10,
        borderRadius: 0,
        backgroundColor: "#fff",
        borderTopWidth: 1,
        backgroundColor: "#f60808",
        borderBottomWidth: 1,
        borderBottomColor: "#f60808",
    },
    btnTextStyle: {
        color: "#fff",
    },
});
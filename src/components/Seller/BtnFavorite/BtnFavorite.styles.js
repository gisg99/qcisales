import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        position: "absolute",
        top: 0,
        right: 0,
        backgroundColor: "#FFF",
        padding: 5,
        paddingLeft: 10,
        borderBottomLeftRadius: 20,
    },
});